The following packages are known to depend on or use graphviz:

Requires
	webdot
	doxygen
	R
	ImageMagick

Require - but just use dot's stdin/stdout simplisticly
	perl-GraphViz
	perl-Graph-Writer-GraphViz
	perl-Graph-SocialMap
	rpmDirectoryCheck,

BuildRequires only
	libxml++ (doxygen, graphviz)
	icu (doxygen, graphviz)
	taglib  (doxygen, graphviz)
